install editable: https://pip.pypa.io/en/stable/topics/local-project-installs/#editable-installs
    hatch build
    python -m pip install F:\Projects\Com\GitHub\privateprefs

publish
    update __about__.py version
    hatch build
    hatch publish

pypi usage
    privateprefs save foo bar
    privateprefs load foo
    privateprefs list
    privateprefs delete foo
    privateprefs delete --all

local usages
    # cd to privateprefs/privateprefs/_cli dir
    python cli.py save foo bar
    python cli.py load foo
    python cli.py list
    python cli.py delete foo
    python cli.py delete --all

# run pytest coverage
    pytest --cov=privateprefs tests/

# run pytest coverage (with missing line numbers)
    pytest --cov-report term-missing --cov=privateprefs tests/
